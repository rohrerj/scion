global:
  scrape_interval: 1s

scrape_configs:
  - job_name: "hummbwtester-client"
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/clients.json

  - job_name: "scion-border-routers"
    file_sd_configs:
      - files:
          - /etc/prometheus/targets/border_routers.json
